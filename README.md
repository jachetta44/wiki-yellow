# Wiki Yellow

Vite + React + TypeScript starter structure for the Wiki Yellow golf guessing game.

Recommended setup:

```bash
npm create vite@latest wiki-yellow -- --template react-ts
cd wiki-yellow
npm install
npm install framer-motion lucide-react html-to-image
```

Then add Tailwind + shadcn/ui and replace the src folder with the files from this package.
